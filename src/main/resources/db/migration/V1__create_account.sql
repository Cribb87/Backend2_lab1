CREATE TABLE user(id INT NOT NULL AUTO_INCREMENT, name VARCHAR(50), age INT, PRIMARY KEY(id));
CREATE TABLE account(id INT NOT NULL AUTO_INCREMENT, userId INT, balance DOUBLE, PRIMARY KEY(id), FOREIGN KEY(userId) REFERENCES user(id));